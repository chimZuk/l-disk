import { ImageElementDirective } from './image-element.directive';

describe('ImageElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageElementDirective();
    expect(directive).toBeTruthy();
  });
});
